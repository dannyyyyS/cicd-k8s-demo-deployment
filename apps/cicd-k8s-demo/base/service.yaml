apiVersion: v1
kind: Service
metadata:
  namespace: demo
  name: cicd-k8s-demo-service
spec:
  type: ClusterIP
  selector:
    app: cicd-k8s-demo
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
