apiVersion: v1
kind: Service
metadata:
  namespace: demo
  name: cicd-demo-service
spec:
  type: ClusterIP
  selector:
    app: cicd-demo
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
